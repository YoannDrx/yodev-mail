import { Activity, Braces, FileCheck2, MailCheck, Paperclip, ShieldCheck } from "lucide-react";
import { PageShell } from "@/components/page-shell";
const features=[[Braces,"API transactionnelle stricte","Un destinataire, une catégorie approuvée, une clé idempotente et des statuts normalisés."],[FileCheck2,"Templates relus","Les templates sont liés à un cas d’usage. Toute modification de contenu déclenche une nouvelle revue."],[MailCheck,"Domaines accompagnés","Yodev prépare DKIM, Return-Path et DMARC, puis active le service de livraison après vérification réelle."],[Paperclip,"Pièces jointes sûres","URL présignée, checksum, contrôle MIME, scan antivirus et suppression au plus tard après 24 heures."],[Activity,"Délivrabilité visible","Livraisons, bounces, plaintes, suppressions, quotas et alertes dans un cockpit sans tracking comportemental."],[ShieldCheck,"Anti-abus natif","Bêta sur dossier, quotas 50/200/500, suspension immédiate et audit de chaque décision."]];
export default function FeaturesPage(){return <PageShell eyebrow="Produit" title="La livraison transactionnelle, sans compte fournisseur à opérer." intro="Mail by Yodev prend en charge l’infrastructure et vous donne une interface unique, réservée aux événements applicatifs légitimes."><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{features.map(([Icon,title,text])=>{const I=Icon as typeof Activity;return <article key={title as string} className="rounded-3xl border bg-white p-7"><I className="size-6 text-primary"/><h2 className="mt-5 text-xl font-semibold">{title as string}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{text as string}</p></article>})}</div></PageShell>}
