import Link from "next/link";
import { Check } from "lucide-react";
import { PageShell } from "@/components/page-shell";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
export const metadata={title:"Tarifs"};
export default function PricingPage(){return <PageShell eyebrow="Tarif bêta" title="Un abonnement d’exploitation, puis l’usage réellement accepté." intro="Pas de checkout public : l’abonnement est créé seulement après validation du dossier."><div className="mx-auto max-w-xl"><article className="relative rounded-3xl border border-primary bg-white p-8 shadow-xl shadow-primary/10"><Badge className="absolute right-6 top-6">Bêta privée</Badge><h2 className="text-2xl font-semibold">Accès plateforme</h2><p className="mt-4 text-4xl font-semibold">29 €<span className="text-sm font-normal text-muted-foreground"> HT/mois</span></p><p className="mt-3 text-lg font-medium">+ 0,0025 € HT par email accepté</p><p className="mt-1 text-sm text-muted-foreground">Soit 2,50 € HT pour 1 000 emails.</p><ul className="mt-7 grid gap-3 text-sm">{["2 domaines","3 membres","Pièces jointes incluses en V1","Accompagnement DNS","Aucun email simulé ou refusé avant acceptation facturé"].map((item)=><li className="flex gap-2" key={item}><Check className="size-4 text-emerald-600"/>{item}</li>)}</ul><Button asChild className="mt-7 w-full"><Link href="/inscription">Candidater</Link></Button></article></div><p className="mt-8 text-center text-sm text-muted-foreground">TVA non applicable, article 293 B du CGI.</p></PageShell>}
