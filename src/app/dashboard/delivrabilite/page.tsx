import { Activity, CheckCircle2, ShieldAlert } from "lucide-react"; import { DashboardPage } from "@/components/dashboard-page"; import { Progress } from "@/components/ui/progress";
export default function Page(){return <DashboardPage title="Délivrabilité" description="Authentification, réputation et seuils de protection par workspace."><div className="grid gap-5 md:grid-cols-3">{[[CheckCircle2,"Authentification","SPF, DKIM et MAIL FROM alignés","text-emerald-600"],[Activity,"Réputation SES","Signaux sains sur 30 jours","text-emerald-600"],[ShieldAlert,"Warm-up","Étape 3 · 2 500 emails/jour","text-amber-600"]].map(([Icon,a,b,c])=>{const I=Icon as typeof Activity;return <article className="rounded-2xl border bg-white p-6" key={a as string}><I className={`size-6 ${c}`}/><h2 className="mt-4 font-semibold">{a as string}</h2><p className="mt-2 text-sm text-muted-foreground">{b as string}</p></article>})}</div><div className="mt-6 rounded-2xl border bg-white p-6"><h2 className="font-semibold">Seuils automatiques</h2><div className="mt-6 grid gap-6 md:grid-cols-2">{[["Hard bounces","0,71 %","Alerte 2 % · Pause 5 %",14],["Plaintes","0,03 %","Alerte 0,1 % · Pause 0,2 %",15]].map(([a,b,c,d])=><div key={a as string}><div className="flex justify-between"><span>{a as string}</span><strong>{b as string}</strong></div><Progress value={d as number} className="my-3"/><p className="text-xs text-muted-foreground">{c as string}</p></div>)}</div></div></DashboardPage>}
